#include <stdio.h>

/**
 * main - using puts, will print a string of characters to the output
 *
 * Return: returns 0
 */

int main(void)
{
	char Mystr[] = "\"Programming is like building a multilingual puzzle";

	puts(Mystr);
	return (0);
}
