#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - function will print the last digits of random strings
 *
 * Return: 0 returned for success
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	
}
